.DS_Store

/api/vendor
/api/.env
/api/storage/*.key
/api/public/storage
/api/public/hot

/web/node_modules
/web/.nuxt
/web/.output
/web/.env

.idea/workspace.xml
.idea/shelf

# Map tiles: ~32 MB, cut from an external daily build. Rebuild with
# tools/fetch-tiles.sh instead of committing them.
/web/public/tiles/*.pmtiles
